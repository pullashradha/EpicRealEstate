import { distanceMatrix } from 'house-search/helpers/distance-matrix';
import { module, test } from 'qunit';

module('Unit | Helper | distance matrix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = distanceMatrix([42]);
  assert.ok(result);
});
