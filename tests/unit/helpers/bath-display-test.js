import { bathDisplay } from 'house-search/helpers/bath-display';
import { module, test } from 'qunit';

module('Unit | Helper | bath display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bathDisplay([42]);
  assert.ok(result);
});
